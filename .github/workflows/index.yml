on:
  workflow_call:
    inputs:
      repo_name:
        required: true
        type: string
      pr_number:
        required: true
        type: number
    secrets:
      GITHUB_AUTH_TOKEN:
        required: true

jobs:
  main:
    # if: ${{ !startsWith(github.head_ref, 'release-please') && !startsWith(github.head_ref, 'renovate') }}
    runs-on: ubuntu-20.04
    steps:
      - uses: octokit/request-action@v2.x
        id: get_pull_request_info
        with:
          route: GET /repos/{repo}/pulls/{pr}
          repo: ${{ inputs.repo_name }}
          pr: ${{ inputs.pr_number }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_AUTH_TOKEN }}

      - name: Parse pull request using jq and checking mergeable state
        id: mergeable_pr
        run: |
          cat <<EOF > pr.js
          ${{ steps.get_pull_request_info.outputs.data }}
          EOF
          export IS_MERGEABLE_PR=$(jq -r '.mergeable' pr.js)
          export BASE_BRANCH=$(jq -r '.base.ref' pr.js)
          export COMPARE_BRANCH=$(jq -r '.head.ref' pr.js)
          echo "mergeable=$IS_MERGEABLE_PR" >> $GITHUB_OUTPUT
          echo "baseBranch=$BASE_BRANCH" >> $GITHUB_OUTPUT
          echo "compareBranch=$COMPARE_BRANCH" >> $GITHUB_OUTPUT

      - uses: actions/checkout@v3
        if: steps.mergeable_pr.outputs.mergeable
        with:
          ref: origin/${{ steps.mergeable_pr.outputs.baseBranch }}

      - name: Merge the PR
        if: steps.mergeable_pr.outputs.mergeable
        run: |
          git remote set-url origin https://cerebrumcom:${{ secrets.GITHUB_AUTH_TOKEN }}/${{ inputs.repo_name }}.git
          git config --global user.email "smellen@cerebrum.com"
          git config --global user.name "cerebrumcom"
          git merge --ff-only ${{ steps.mergeable_pr.outputs.compareBranch }}
          git push
